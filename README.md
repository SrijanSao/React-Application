#ReactApp

To get the app working and show the contribution folllow the below steps:

1. install git and clone the repository: 'https://github.com/yashkumar6640/ReactApp.git'

2. Go to the directory 'ReactApp' from command line and run 'npm install && npm start'

3. Open another command prompt from the same directory and run 'webpack -w'

